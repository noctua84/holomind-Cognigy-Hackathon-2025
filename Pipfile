[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
torch = ">=1.9.0"
numpy = ">=1.21.0"
pandas = ">=1.3.0"
matplotlib = ">=3.4.0"
seaborn = ">=0.11.0"
tensorboard = ">=2.8.0"
psutil = ">=5.8.0"
pymongo = ">=4.0.0"
psycopg2-binary = ">=2.9.0"
pyyaml = ">=6.0"
tqdm = ">=4.62.0"

[dev-packages]
pytest = ">=7.0.0"
pytest-cov = ">=3.0.0"
isort = ">=5.10.0"
pylint = ">=2.14.0"
pre-commit = ">=2.19.0"

[requires]
python_version = "3.13"

[scripts]
test = "pytest"
lint = "pylint src tests"
sort = "isort ." 